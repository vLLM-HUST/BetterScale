# Patches

No patches applied yet. Preserve an unchanged baseline first.

For each measured change, keep its patch against one pinned upstream together
with a short rationale and exact reproduction/evidence reference. Name the
target component and prerequisite patches. Check with `git apply --check`
before applying, and keep independent changes independently reversible.

Do not add a plugin skeleton until a concrete integration requires it.
